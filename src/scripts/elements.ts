export const containerButtons = document.querySelector(
  "#categoryList"
) as HTMLDivElement;

export const moviesTable = document.querySelector(
  "#moviesTableBody"
) as HTMLTableElement;
export const countMovie = document.querySelector(
  "#movieCount"
) as HTMLSpanElement;
export const searchInput = document.querySelector(
  "#searchInput"
) as HTMLInputElement;
export const paginationContainer = document.querySelector(
  "#pagination"
) as HTMLUListElement;
export const pageTitle = document.querySelector("#pageTitle") as HTMLHeadingElement;
